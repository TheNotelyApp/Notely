/**
 * Notely AI Frontend Exports
 */

const { IPC_EVENTS, AIQueryRequest, AIQueryResponse } = require('./utils/ipcProtocol');

module.exports = {
  IPC_EVENTS,
  AIQueryRequest,
  AIQueryResponse
};
