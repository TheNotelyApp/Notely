name: CD

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build Windows Executables
      shell: bash
      env:
        CSC_LINK: ${{ secrets.CSC_LINK }}
        CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
      run: ./build-windows-exe.sh

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        generate_release_notes: true
        body: |
          ## Download

          | File | Type | Notes |
          |------|------|-------|
          | `Notely Setup *.exe` | **NSIS Installer** (recommended) | Installs to system with start menu / desktop shortcuts |
          | `Notely *.exe` | **Portable Standalone EXE** | Single all-in-one executable, zero installation required |
          | `Notely-*-win.zip` | **Portable ZIP** | Portable ZIP package — extract and run anywhere |
        files: |
          release/*.exe
          release/*.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
